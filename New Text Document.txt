cd D:\
mkdir packages
mkdir $Env:UserName

cd packages
mkdir npm
setx /M npm_config_cache D:\packages\npm
Copy-Item "%AppData%\npm-cache\*" -Destination "%NPM_CONFIG_CACHE%"

setx /M VCPKG_DEFAULT_BINARY_CACHE D:\packages\vcpkg

# cd ../$Env:UserName
# mkdir .nuget\